{
  "name": "puyoquest",
  "version": "1.0.0",
  "engines": {
    "node": ">=16.6.0"
  },
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "find ./packages -name 'lib' -type d -prune -exec rm -rf '{}' + && find ./packages -name 'tsconfig.tsbuildinfo' -prune -exec rm -rf '{}' +"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "ts-node": "^10.0.0",
    "ts-node-dev": "^1.1.8"
  }
}
